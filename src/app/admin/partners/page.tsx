import NotInMvp from "@/features/admin/components/NotInMvp/NotInMvp"

export default function AdminPartnersPage() {
  return (
    <NotInMvp
      title="Partners"
      purpose="Sponsor records and tier assignment — the sponsors table that the sponsor_stats view joins against. Sponsor names on the dashboard are hard-coded until this lands."
    />
  )
}
